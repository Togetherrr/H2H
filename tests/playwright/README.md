# Playwright

Thư mục này dành cho source e2e test với Playwright.

- `e2e/`: chứa các test case `.spec.ts`
- `fixtures/`: chứa test data hoặc helper cho e2e

Khi thêm Playwright sau này, có thể đặt `playwright.config.ts` ở root và trỏ testDir vào `tests/playwright/e2e`.
